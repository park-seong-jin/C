#include <stdio.h>//Standard Input Output Header

void main(void) {

	int sum = 0;
	int num = 1;

	while (num <= 5) {
		printf("num(%d) + sum(%d) = ",num,sum);
		sum = sum + num;
		printf("%d\n",sum);
		num++;
	}

	printf("\nResult : num = %d , sum = %d", num, sum);
}
